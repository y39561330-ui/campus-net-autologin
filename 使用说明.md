# 校园网自动登录 · 使用说明书

> 这份说明书是写给**第一次用、不太懂电脑**的同学看的。
> 照着一步步做就行，遇到问题先看第 6 节「常见问题」。

**它能帮你做什么**：以后开机登录 Windows 后，电脑会**自己完成校园网认证**，不用再打开浏览器、等页面跳转、手输账号密码。断网了也会自动重连。

**适用**：北京信息科技大学，Windows 10 / 11 电脑，插网线或连校园 Wi-Fi（`BISTU`）都行。

---

## 1. 安装前，先确认三件事

| 检查项 | 说明 |
| --- | --- |
| **① 解压，不要直接双击压缩包里的文件** | 右键压缩包 →「全部解压缩」，解压到一个文件夹里。脚本之间要互相调用，只拖出单个文件会出错。 |
| **② 输入法切到英文（半角）** | 输入密码时如果用中文全角状态，打出的 `！＠＃` 和 `!@#` 看着一样但其实是不同字符，密码必然错误。脚本会实时提醒你。 |
| **③ 准备好学号和密码** | 就是你平时在认证页面登录用的那一组。 |

---

## 2. 安装（大约 1 分钟）

### 第 1 步：双击 `install.cmd`

打开解压后的 `campus-net` 文件夹，双击 **`install.cmd`**。

> **如果弹出蓝色提示"Windows 已保护你的电脑"**：点「更多信息」→「仍要运行」。
> 这是因为脚本没有数字签名，不是病毒。**如果杀毒软件报警**：选择「允许」或把该文件夹加入信任列表。
> 原理说明见本文件第 8 节，脚本只往学校的认证服务器发请求，不会上传任何东西。

### 第 2 步：输入学号

```
请输入校园网账号
```

输入你的学号，按回车。

> 如果你是**第二次**安装（比如改过密码），直接回车就会沿用上次的学号。

### 第 3 步：选择登录方式

```
请选择登录方式：
  1) 校园用户（大多数同学选这个，账号后面不加后缀）
  2) 校园电信（账号后加 @dx）
  3) 校园联通（账号后加 @lt）
输入 1/2/3（直接回车默认 1）
```

**绝大多数同学直接按回车**即可。只有你自己办了电信/联通的宽带套餐才需要选 2 或 3。

### 第 4 步：输入密码 ← 最容易出错的一步

你会看到：

```
请输入校园网密码
   · 默认显示为 *，按 F2 切换【显示明文 / 重新隐藏】
   · 支持 Ctrl+V 粘贴；Backspace 删一个字符；回车确认；Esc 取消
   · 请先把输入法切到英文（半角），全角字符会导致密码不对
  > 
```

| 操作 | 效果 |
| --- | --- |
| 直接打字 | 显示成一串 `*` |
| **按 F2** | **把密码显示成明文**（再按一次又隐藏）—— 怕打错就先看一眼 |
| Ctrl + V | 粘贴（推荐从密码管理器粘贴，避免手打错） |
| Backspace | 删掉一个字符 |
| 回车 | 确认 |
| Esc | 取消安装 |

输入时如果出现 `【全角字符 1 个】` 或 `【空格 2 个】` 这样的红字提醒，**说明你打错了**——那是全角符号或多余空格，请删掉重新输入。

### 第 5 步：看验证结果

输入密码后，脚本会**立刻**拿它去学校服务器验证一次（不会等到重启才验证）：

| 屏幕上显示 | 含义 | 怎么办 |
| --- | --- | --- |
| `验证通过：认证成功` | 账号密码正确 | 继续下一步 |
| `验证失败：门户返回「用户名或密码错误」` | 密码打错了 | 会让你**当场重新输入**（最多 4 次） |
| `无法确认结果（门户没有返回明确成败）` | 网络刚断开或门户临时不通 | 继续安装，之后看日志 |

> 小提示：如果你电脑**此刻已经在线**，脚本会额外提醒"这个验证结果仅供参考"——因为门户对已登录的 IP 有时会直接返回成功。想严格确认，可以先到 `https://lan.bistu.edu.cn` 点「注销」，再重新运行安装。

### 第 6 步：UAC 弹窗 → 点「是」

会弹出「你要允许此应用对你的设备进行更改吗？」，点 **「是」**。

这是用来注册"断线自动重连"的计划任务。**不想授权也可以点「否」**，脚本会自动改用后台守护方式，功能一样。

### 第 7 步：是否让"锁屏时也有网"？（可选）

```
可选：让"开机后、还没登录 Windows（锁屏界面）"的那段时间也有网。
      做法：把你的密码再存一份"机器级加密"的副本，交给 SYSTEM 身份的开机任务使用。
      代价：这台电脑上的其他账户理论上也能解密这份副本（用户级那份不受影响）。
要启用吗？(Y/n)
```

| 你的情况 | 建议 |
| --- | --- |
| 电脑就你一个人用 | 可以选 **Y**，好处是开机到锁屏界面就已经有网，登录后立刻能上网 |
| 电脑有别人用 / 有多个账户 | 建议选 **n**（输入 n 回车），只保留需要登录后才生效的那种 |

### 第 8 步：安装完成

看到 `安装完成。` 就成功了。桌面上会多出一个 **「连接校园网」** 快捷方式。

---

## 3. 装好之后，日常怎么用

**大部分时候你什么都不用做。**

| 场景 | 会发生什么 |
| --- | --- |
| 开机 / 重启 | 登录 Windows 后约 **5 秒**自动完成认证 |
| 拔网线 / 插网线 | 几秒内自动识别并重新认证 |
| 从有线切到校园 Wi-Fi（`BISTU`） | 自动识别无线网卡并认证 |
| 合盖睡眠后唤醒 | 自动重新认证 |
| 平时 | 后台每 60 秒轻量检查一次，掉线自动重连 |
| 连手机热点 / 家里路由器 | 探不到学校门户就**安静跳过**，不会捣乱 |

**想手动重连**：双击桌面 **「连接校园网」**。

- 认证成功 → 窗口显示结果，**3 秒后自动关闭**
- 没成功 → 窗口**停住显示原因**，看完按任意键关闭（方便你截图求助）

---

## 4. 怎么确认它真的在工作

打开这个文件（记事本就能开）：

```
%LOCALAPPDATA%\CampusNetAutoLogin\log.txt
```

> 快速打开方法：按 `Win + R`，粘贴 `%LOCALAPPDATA%\CampusNetAutoLogin`，回车，就能看到 `log.txt`。

正常的日志长这样：

```
2026-09-18 12:27:39 [INFO] 尝试 以太网 有线：10.145.145.232
2026-09-18 12:27:39 [INFO] 认证返回：Portal协议认证成功！
2026-09-18 12:27:41 [INFO] 校园网已连通，认证完成。
```

判断方法：**看最后几行的时间戳**。如果时间是你刚开机的时间，说明它在正常工作。

---

## 5. 卸载

双击 `campus-net/uninstall.cmd`，会删除：

- 计划任务（含开机任务）、开机启动项、桌面快捷方式
- `%LOCALAPPDATA%\CampusNetAutoLogin`（**包括加密保存的密码**）
- `C:\ProgramData\CampusNetAutoLogin`

删除开机任务需要管理员权限，脚本会自动请求一次 UAC，点「是」即可。

---

## 6. 常见问题

### Q1：总提示「用户名或密码错误」，但我确定密码是对的

按顺序排查：

1. **双击 `test-password.cmd`** —— 它会把你输入的密码**逐字符列出来**（每个字符的 Unicode 编码），全角字符、空格会被标★。这是最有效的排查手段。
2. 检查**输入法是不是全角**。`！`（全角）和 `!`（半角）是两个不同字符。
3. 检查**登录方式选错**：如果你的账号是电信/联通套餐，安装时要选 2 或 3。
4. 检查密码里**有没有多余空格**（从网页复制时容易带上）。

### Q2：开机后要等几秒才有网，正常吗？

正常。登录 Windows 后约 5 秒开始认证。**锁屏界面（还没登录）那段时间是否有网**，取决于你第 7 步选没选「开机任务」。

### Q3：改了校园网密码怎么办？

重新运行一次 `install.cmd`：学号会自动沿用上次的，只需输入新密码。

### Q4：浏览器所有网站都打不开，但微信、QQ 正常

这**不是校园网的问题**，一般是你用的代理工具（Clash / Clash Verge 等）退出时没关掉"系统代理"，留下了 `127.0.0.1:7897` 这类已经没人监听的地址。

**解决**：双击 `tools/fix-proxy/fix-proxy.cmd`，然后把浏览器**完全关掉再打开**。

以后用完代理工具，记得**先关掉它的"系统代理"开关再退出**。

### Q5：偶尔看到黑色窗口闪一下

本工具默认用 `wscript` 无窗口方式启动，正常不会闪。如果你看到闪窗，说明你装的是旧版本——重新运行一次 `install.cmd` 覆盖即可。

### Q6：会不会把我的密码传到网上？

不会。密码用 **Windows DPAPI** 加密后保存在你自己电脑上，**只有本机、只有你这个 Windows 账户**能解密（拷到别的电脑都解不开）。脚本唯一会连接的地址是**学校的认证服务器**。

不过要注意：如果你在第 7 步启用了"开机任务"，密码会**额外**存一份机器级加密的副本（这是让锁屏时也有网的代价），**同一台电脑上的其他账户理论上能解密**。介意的话就别启用它。

### Q7：我换宿舍 / 换网口 / IP 变了，要重装吗？

不用。脚本每次运行都会重新读取当前网卡的 IP 和 MAC，IP 变化、换网线、DHCP 续约都不影响。

### Q8：装完发现不想要了

双击 `uninstall.cmd`，见第 5 节。

### Q9：`BISTU-802.1X` 这个 Wi-Fi 需要装吗？

不需要。那是企业级认证（802.1X），Windows 连网时自己就完成了，跟本工具无关。需要本工具的是 `BISTU` 这个 SSID。

---

## 7. 出问题了怎么求助

发给同学或维护者时，请附上这两样（**不要发密码**）：

1. **日志的最后 20 行**：`%LOCALAPPDATA%\CampusNetAutoLogin\log.txt`
2. **网卡与门户体检结果**，在 PowerShell 里运行：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:LOCALAPPDATA\CampusNetAutoLogin\Connect-CampusNet.ps1" -Probe
```

它会打印：当前认证门户、候选网卡（哪张是有线/无线、IP、MAC、SSID）、门户是否可达、当前联网状态。

---

## 8. 附：它是怎么工作的（给好奇的同学）

学校用的是 **Dr.COM（哆点）** 认证系统。你打开浏览器时被网关拦截、跳到认证页，而页面上的"登录"按钮，本质上就是**发一个 HTTP 请求**。网关是按「**IP + MAC**」放行的，所以把这个请求直接发出去，效果和手动点登录完全一样，**不需要浏览器**。

几个细节：

- 脚本不写死用哪张网卡，而是问系统"**要去认证服务器时实际从哪张网卡出去**"，所以有线和无线都能自动适应；连无线时会自动带上 Wi-Fi 名称参数。
- 门户其实有**两个**登录服务：新版（801 端口，返回 JSON）和登录页自己用的老版（80 端口）。如果密码里有 `&` `<` `>` 这类字符，新版接口会被过滤器拦下，脚本会自动改用老版通道。
- 判断"有没有网"不能用 `generate_204` 之类的探测地址——学校网关把它们列进了白名单，没认证也可能返回正常结果，会误判。所以脚本改用 HTTPS 访问必应/腾讯/百度并校验内容。
- 整个流程：**先检查能不能上网 → 不能才提交认证 → 认证成功后等网关真正放行 → 再确认联网**。所以不会没事就重复登录。

**加密与权限**：密码用 `ConvertFrom-SecureString`（Windows DPAPI）加密；可选的开机任务用机器级 DPAPI，并把配置目录权限收紧为「SYSTEM / 管理员完全控制，普通用户只读」，防止普通账户替换 SYSTEM 要执行的脚本。

---

## 9. 提醒

- 请使用**你自己的**账号密码。
- 使用前请自行确认符合学校的网络使用管理规定。
- 本工具按"现状"提供，因使用产生的任何后果由使用者自行承担（详见 `LICENSE`）。
