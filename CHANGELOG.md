# 更新日志

## v1.1.0 · 2026-09-24

### 修复：进了桌面偶尔还得自己手动连一次

**实测数据**（修复前，每天开机的时间戳）：

| 日期 | 桌面出现 | 认证完成 | 守护进程启动 |
| --- | --- | --- | --- |
| 09-21 | — | 12:31:11 | 12:32:04（晚 53 秒） |
| 09-22 | — | 12:26:24 | 12:27:18（晚 54 秒） |
| 09-23 | — | 12:26:37 | 12:26:52（晚 15 秒） |
| 09-24 | 09:48:27 | 09:50:21 | 09:50:37（**晚 130 秒**） |

**根因**：守护进程原本靠"启动文件夹"拉起，而 Windows 要等 Explorer 加载完才执行它，实测比桌面出现晚 **15~130 秒**；这段空档里**没有任何进程在盯着网络**。另外，一次性任务如果跑在网卡 / IP 就绪之前，旧代码会直接放弃、不重试。

### 本次改动

- **登录后 2 秒就拉起守护进程**（原为 5 秒），不再依赖启动文件夹的时机
- 新增 **锁屏解锁** 与 **睡眠唤醒** 触发 —— 登录触发器不会因为"解锁"而触发，之前锁屏一晚、早上解锁是不受管的
- 守护进程改为 **单例 + 自愈**：计划任务也用 `-Watch` 启动；已有守护进程就自动退出，被杀掉 / 崩溃就自动补上
- **不再"太早就放弃"**：网卡 / IP 还没就绪时，会在本轮内反复重试，而不是直接返回失败
- **联网判断加固，防止误判"已在线"而跳过认证**：
  - 新增门户页识别（网关可能把未认证流量劫持成一个 200 的门户页，旧逻辑只看"200 + 内容够长"，会被骗过）
  - 校验返回内容特征（必应含 `bing`、腾讯含 `qq`、百度含 `baidu`）
- 并发互斥从"静默跳过"改为 **等待 + 记录日志**（以前出现并发跳过时日志里查不到，无法定位）
- 不在校园网时**提前结束本轮**，交给守护进程稍后重试，避免白跑满超时、刷日志
- 守护进程启动日志里带上版本号，便于确认在跑哪一版

## v1.0.0 · 2026-09-23

首个正式版：

- 校园网自动登录（开机自动认证 / 断线重连 / 有线与校园 Wi-Fi 自适应 / 密码 DPAPI 加密 / 可选 SYSTEM 开机任务 / 全程无窗口）
- 浏览器代理一键修复工具（解决 Clash 等退出后残留死代理导致的"浏览器全打不开"）
- 《使用说明.md》手把手说明书 + 5 张效果演示图
